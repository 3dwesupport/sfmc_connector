{
  "name": "karix-middleware",
  "version": "1.0.0",
  "description": "Middleware API that receives a batch of campaign members from SFMC and sends messages via the Karix API",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "dependencies": {
    "axios": "^1.7.2",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "p-limit": "^3.1.0"
  }
}
